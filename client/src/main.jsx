// Точка входа клиента — будет заполнена в задаче 17
import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div>1xBetLineBoom — инициализация...</div>
  </React.StrictMode>
);
