// Точка входа сервера — будет заполнена в задаче 2
import 'dotenv/config';

const PORT = process.env.PORT || 3001;

console.log(`Сервер будет запущен на порту ${PORT}`);
console.log('Инициализация модулей...');
